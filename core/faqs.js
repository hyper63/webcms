const { Async } = require('crocks')

module.exports = (services) =>
  ({
    list: () => {
	
    },
    create: (faq) => null,
    update: (id, faq) => null,
    get: (id) => null,
    'delete': (id) => null
  })
